<IfModule mod_rewrite.c>
  RewriteEngine On

  # Serve existing files and directories normally; send client-side routes to Vite's entry point.
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
